public class PrintStream {
    public static void main(String[] args) {
        /*
        1. In System.out.println()  the [ out  ] is known as the variable of PrintStream
           and the PrintStream have the method called  [ println() ]
            [imp] anything we print in the println() which calls the toString()
        2. pretty printing in java string (search in google)

   String:  %s - String
            %d - Integer
            %f - Floating-point number
            %b - Boolean
            %c - Character
            %x - Hexadecimal integer

Padding and Alignment :
            %-<width>s - Left-align within specified width
            %<width>s - Right-align within specified width
            %0<width>d - Zero-padding for integers

Decimal Formatting

            %.<precision>f - Fixed-point notation with specified decimal places
Using System.out.printf()

            Same format specifiers as String.format()

 Using StringBuilder or StringJoiner for custom formatting

Using JSON libraries (like Gson) for pretty printing

.setPrettyPrinting() for formatted JSON output



         */



    }
}
